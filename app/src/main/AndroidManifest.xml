<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Pantrix needs this to report network metadata. Without it NetworkChangeCollector.register()
         returns without registering its callback, so every event carries the NOT_CONNECTED default and
         no network_change event is ever emitted. Normal permission: no runtime prompt.
         Needed here because this app is pinned to 1.0.0-beta.5, whose manifests declare no permissions
         at all. From 1.0.0-beta.6 the SDK declares it itself and this line becomes redundant — harmless
         either way (the merger dedupes), so it can stay or go when the pin moves. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".app.RortyApp"
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pantrixrortyanddemo">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Pantrixrortyanddemo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>